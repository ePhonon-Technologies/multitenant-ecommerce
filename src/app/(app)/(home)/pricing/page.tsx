// Page - Functional component for the Pricing route (WIP)
const Page = () => {
  return <div>Pricing Page</div>; // Placeholder content for the Pricing page
};

export default Page; // Export the component as the default for routing
