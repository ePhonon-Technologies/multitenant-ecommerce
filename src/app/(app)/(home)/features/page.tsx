// Page - Functional component for the Features route (WIP)
const Page = () => {
  return <div>Features Page</div>; // Placeholder content for the Features page
};

export default Page; // Export the component as the default for routing
